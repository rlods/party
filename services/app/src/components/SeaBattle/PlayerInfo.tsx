import React from "react";

// ------------------------------------------------------------------

export const PlayerInfo = ({ info }: { info: string }) => (
	<div className="SeaBattlePlayerInfo">{info}</div>
);
